# machinemax-assignment
# Prerequisite
 - open terminal and set LORAWAN_HOST value by following command
 ```sh
  export LORAWAN_HOST=http://lorawan_host
 ```
# Run the project
```sh
 go run main.go
```
